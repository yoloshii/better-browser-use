# Core
aiohttp>=3.13
pydantic>=2.12
markdownify>=1.2
pyee>=13,<14
python-dotenv

# Tier 1 — Playwright (Chromium)
playwright>=1.51,<1.56

# Tier 2 — CloakBrowser (C++ patched Chromium, primary)
cloakbrowser>=0.3.18

# Tier 2 — Patchright (optional fallback)
# patchright

# Tier 3 — Camoufox (anti-detect Firefox)
# camoufox[geoip]
